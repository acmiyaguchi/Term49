#ifndef PLATFORM_H_
#define PLATFORM_H_

/* Backend-agnostic platform handle. Platform implementations own raw event
 * sources and OS services such as clipboard, notifications, invocation, and
 * virtual keyboard integration. */
typedef struct t49_platform t49_platform_t;

#endif
