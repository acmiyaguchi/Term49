#ifndef RENDERER_H_
#define RENDERER_H_

/* Backend-agnostic renderer handle. Concrete renderer implementations own
 * windows, surfaces/buffers, fonts, glyph caches, and render caches. */
typedef struct t49_renderer t49_renderer_t;

#endif
