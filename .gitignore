*.bar
Device-Debug
Device-Release
src/*.o
src/*.d
tools/**/*.o
tools/**/*.d
vendor/qrcodegen/*.o
vendor/qrcodegen/*.d

# Device credentials for `make deploy`/`connect` (see .env.example).
.env

# bbnix bundle staged by `make stage-bbnix`; keep only the placeholder so the
# bar-descriptor asset path always exists.
share/bbnix/*
!share/bbnix/.keep

# Font bundle staged by `make stage-fonts`; keep only the placeholder so the
# bar-descriptor asset path always exists.
share/fonts/*
!share/fonts/.keep

result
result-bbnix
result-fonts
