*.bar
Device-Debug
Device-Release
src/*.o
src/*.d
tools/**/*.o
tools/**/*.d
signing/bbpass
signing/*.csk
signing/*.bar
signing/*.p12
signing/ssh-key*

# bbnix bundle staged by `make stage-bbnix`; keep only the placeholder so the
# bar-descriptor asset path always exists (issue #43).
share/bbnix/*
!share/bbnix/.keep
result
result-bbnix
