*.bar
Device-Debug
Device-Release
src/*.o
src/*.d
tools/**/*.o
tools/**/*.d

# Device credentials for `make deploy`/`connect` (see .env.example).
.env

# bbnix bundle staged by `make stage-bbnix`; keep only the placeholder so the
# bar-descriptor asset path always exists.
share/bbnix/*
!share/bbnix/.keep
result
result-bbnix
